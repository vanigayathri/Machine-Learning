Clustering Algorithm:
K-means is a popular unsupervised machine learning algorithm used for clustering, where the goal is to partition a dataset into K distinct, non-overlapping subgroups or clusters.